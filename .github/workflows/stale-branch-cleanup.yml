name: stale-branch-cleanup
# 一次性:刪除 2026-08-01 盤點核可的舊分支(名單見 syntax-field-archive/state/branch-cleanup-2026-08-01.md)
on:
  push:
    branches: [claude/github-branch-cleanup-f4ueii]
permissions:
  contents: write
jobs:
  delete-stale-branches:
    runs-on: ubuntu-latest
    steps:
      - name: Delete vetted stale branches
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -u
          for b in \
            claude/moonyou-rename-guide-tab-JmSQL \
            claude/standalone-cocreation-app-939vhb \
            claude/update-moonyou-links-kmNoh \
          ; do
            if gh api -X DELETE "repos/${GITHUB_REPOSITORY}/git/refs/heads/$b" >/dev/null 2>&1; then
              echo "deleted: $b"
            else
              echo "skip/failed: $b"
            fi
          done
